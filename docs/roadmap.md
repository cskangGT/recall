# Mado 로드맵

> 설계도 묶음: 제품 규칙은 [product-spec.md](product-spec.md), 기억의 원칙은 [memory-charter.md](memory-charter.md),
> 여기는 **무엇을 언제 만들지**. 갱신일: 2026-08-20.

## 지금까지 (배포됨 — http://43.202.24.252, Lightsail 서울)

- **코어**: 3면(둘러보기·지도·원본) · 기하학 게이트 자동 재구성 · 인용 Ask(스트리밍, '기억 자신' 회상체) · 대화 스레드 · 키워드 렌즈 드릴다운 · 유사 기억 합치기(AI 이유 + 무손실)
- **검수 (spec §21)**: 원문↔기억 대조, 유지/빼기/고치기, 모든 평결이 curation 신호로 기록 → 최근 discard가 다음 추출의 부정 예시 (학습 루프 v1)
- **임포트**: 파일 드롭 · Instagram export ZIP(실물 스키마) · Apple Notes · Notion(원천 URL 보존, "원천에서 지워도 안전" 배지)
- **비즈니스**: 2주 무료 창 페이월 · Stripe 테스트 결제 왕복(free→pro 검증) · 초대 토큰 게이트 · 방문자별 워크스페이스 · 로케일별 시드(en 47 / ko 59, 둘 다 실파이프라인으로 저작)
- **i18n**: en/ko 사전 단일화, ko 명명 하드 룰
- **일기**: 하루 단위의 방 (D) — 달력 + 하루 페이지(쓰기·이어쓰기) + 그날 들어온 기억 자동 표시. 일기도 파이프라인을 타서 지도·검색·Ask에 합류. 회고("그때의 나 요약")는 다음 단계

## 다음

- **미팅 (Google 캘린더)** — 완료: OAuth 연결, 게으른 동기화(앞 14일·뒤 7일), 참석자→개체 매칭 + 관련도 검색으로 '미팅에 도움될 기억', 미팅 후 한 줄(원문에 미팅·참석자를 적어 넣어 다음 만남에 개체 매칭으로 돌아옴). 다음: Gmail 맥락(제한 범위·심사 필요), `meeting_id` 연결, 동의 화면 프로덕션 전환.
- **홈 브리핑** — 완료: 오늘 미팅 · 요즘(기억 말투 한 문단, 하루 1회) · 붙잡고 있는 것 · 자라는 중 · 확인 전 원본. 카테고리 목록은 그 아래. (우선순위순)
- **하루 루프** — 완료: ① 첫 하루 온보딩(첫 생각 하나 → Mado가 기억 말투로 읽은 것을 답함 → 캘린더(문이 있을 때만) → 쌓아둔 것 → 홈의 '첫날' 카드) ② 시간대별 홈(같은 블록, 다른 순서 — 아침은 오늘, 낮은 '오늘 3개만 확인', 저녁은 '오늘 들어온 기억'과 일기) ③ 붙잡고 있는 것 '내려놓기'(`settled_at` — 지우지 않고 목록에서만 뺌, 페이지에서 '다시 붙잡기') ④ 주 1회 '지난 주의 나' 카드(일기 회고 재사용) ⑤ 레일을 하루 순서로(미팅 → 둘러보기 → 지도 → 원본 → 일기). 만들지 않기로 한 것: 투두 체크리스트, 스트릭, 푸시 알림, 시간대별 색 변화.
- **네 곳으로 정리한 구조 (2026-09-18)** — 완료: 홈(로고·앱 첫 화면 = 하늘 장면 + 문 넷: 오늘 하루 시작하기 · 내 기억 채우기 · 기억 찾아가기 · 오늘 일기 쓰기) / 오늘(큰 문장 하나 + 일정·마음·정보 세 줄, 한 번에 한 줄, 미팅은 일정 줄 안) / 기억(한 곳, 세 렌즈: 둘러보기 T · 브레인스토밍 G · 기억자료실 S — 같은 하늘 테마, 하단의 같은 '찾기·질문' 바, 폴더 보기는 둘러보기의 옵션) / 일기. 레일의 미팅·지도·원본·질문 버튼은 없앰.
- **골라서 생각하기 (2026-09-23)** — 완료: 브레인스토밍의 모드 스위치(별을 누르면 고르기, 카테고리·개체는 안의 기억 전부, 검색 결과 한꺼번에 고르기), 고른 기억은 별자리(빛·반짝임·실)로 표시, 이것만 펼치기, 고른 기억을 힌트로 대화(`focus`, 거절 없음, 옆 패널에 '내가 고른 것/Mado가 더 가져온 것'), 묶기(새 카테고리 · 기존 카테고리 · 한 메모로 응축). Mado의 카테고리 이름 제안(같은 namer, `POST categories/suggest-name`). 묶은 뒤에도 모드는 이어지고(묶음 이름이 줄에 붙음, 바가 그 묶음으로 대화를 권함) 첫 질문은 버튼('고른 기억 N개로 브레인스토밍 시작하기'), Mado의 답 한 줄을 '{묶음}에 두기'로 기억에 남기면 그 줄 아래에 남긴 기억이 바로 보임. 미룬 것: 드래그 영역 선택.
- **첫 대화 온보딩 (2026-09-24)** — 완료: 다섯 박자(못 정하는 것 하나 → Mado가 되묻고 이유 두세 줄 → 별자리+첫 답 → 한 줄 남기면 첫 카테고리 → 세 줄 설명·네 곳·문). 다음 날 아침 '어제 …을 붙잡고 있었죠' 카드, '요즘'이 '넌 …하는 편이야'로 시작, t()의 자동 조사({은/는}). 되묻는 말은 모델이 사용자의 말 위에 씀(`onboarding/ask-back`, 없으면 고정 문장).
- **돌아오는 링크 (2026-09-24)** — 완료: 계정 전까지 링크가 계정. 설정의 '돌아오는 링크'(`?ws=…&invite=…`), 온보딩 마지막 박자에 한 번, 링크로 열면 그 브라우저가 그 워크스페이스가 됨. 다음: Google로 계속하기(계정, 도메인 뒤).
- **비밀정보 차단 레이어 (2026-09-24)** — 완료: 모든 캡처가 저장·모델 전송 전에 `server/secrets/redact.ts`를 거침(키·토큰·JWT·Bearer·api_key=·카드번호(Luhn)·주민번호는 자리에서 치환, password 줄은 통째로, 주문번호·전화·날짜·가격은 유지). 응답에 `redacted` 수, 토스트로 알림. 리더 3종도 같은 목록. 다음: Google로 계속하기(계정, 도메인 뒤).
- **온보딩 자기소개 (2026-09-24)** — 완료: 첫 줄 "두 번째 '기억'", 질문 전에 '당신을 소개해 줄래요?' 역할 일곱(기획자/PM·창업자/CEO·C-level 임원·연구자·개발자·디자이너·마케터, 건너뛰기 가능). 역할별 예시 2개씩 순환, 되묻기 힌트, 되묻기 프롬프트에 역할 전달. 역할은 `mado.ob.role`에 기억됨.
- **온보딩 미팅 박자 (2026-09-24)** — 완료: 마지막 박자에 '다가오는 미팅 한 줄' → 미팅 메모로 저장 → 지금까지 넣은 기억을 힌트로 실제 답('그 미팅 전에 Mado가 꺼내 두는 것')과 근거 기억을 카드로 보여 줌 → '미팅 뒤 한 줄이 다음 미팅 전에 돌아온다, 캘린더를 연결하면 모든 미팅에' 약속.
- **타겟 문장 (2026-09-24)** — 완료: 자기소개 칩은 예시이지 문이 아님. 칩 아래 '정보를 모으고, 미팅을 하고, 결정을 내리는 일이라면 어떤 직군이든 좋아요' 한 줄, 건너뛰기는 '다른 직군이에요 — 그래도 시작할게요'. 스펙 §2도 같은 말로 넓힘.
- **링크는 통째로, 못 읽으면 이유와 함께 (2026-09-24)** — 완료: 본문(article/main, 네비·푸터 제외) 12,000자까지 캡처로 전달(전에는 body 앞 400자). 카드에 '본문 N자를 읽었어요' / 잘림 안내. 못 읽는 경우를 이름으로: 스크립트 셸(app)·로그인(login)·파일(not-html)·거의 빈 페이지(thin), 실패는 이유(403 등 응답 코드, 시간 초과, 사설망, 연결 실패)를 말로. 직군 칩에 크리에이터/인플루언서, 영업/BD 추가.
- **링크 전체 요약 + 부분 고르기 (2026-09-24)** — 완료: 본문을 결정적으로 절(heading/~1,400자)로 나누고(`server/link/digest.ts`, 모든 줄이 정확히 한 절에), 모델이 전체 요약 1개 + 절마다 1개(개수 강제, 빠지면 첫 문장으로 채움). 카드: 전체 요약 → 별로 켜고 끄는 부분 목록(원문 펼치기) → '내 한 줄' → '{n}부분 기억에 넣기'. 순서가 보고→다듬고→넣기로 바뀜. 다음 후보: 같은 카드를 PDF·파일 드롭에도, 브레인스토밍 '이 말 기억해두기' 편집 후 저장, 온보딩 미팅 한 줄은 답을 본 뒤 저장.
- **파일도 보고 나서 넣기 (2026-09-24)** — 완료: PDF 드롭 → `files/read`(저장+읽기, 소스 없음) → 링크와 같은 카드(`ReadCard`: 전체 요약·부분 별·내 한 줄) → 넣기에서만 소스 생성(읽은 글 + 저장 파일 원본, 두 번 읽지 않음). 텍스트 파일 하나도 같은 카드, 여러 개는 배치 그대로. 여러 PDF는 카드가 차례로.

1. **도메인 + HTTPS** — mado.io/so 등록(유저 액션) → DNS·인증서 → 테스터 링크 교체. *지금 유일하게 막힌 것.*
2. **Stripe 웹훅 실서버 등록** — 대시보드에 호스팅 엔드포인트 추가(도메인 후) → 실서버 결제 왕복
3. **인스타 내보내기 마법사** — 유저 플로우 합의 완료(신청 안내→JSON 강제→대기 배너→드롭). 온보딩 이탈 방지의 핵심
4. **모바일 공유 타깃 (PWA)** — 인스타/어디서든 "공유→Mado". 과거는 ZIP, 앞으로는 공유 = 체감 연동
5. **주간 타임라인 뷰** — 원본 뷰 진화: 주 단위 그룹 + 이 주의 키워드 칩(keywordsFor 재사용). 페이월·다이제스트와 문법 연결
6. **2주 다이제스트** — 재방문 루프. 타임라인 위에 얹음
7. **Mac 메모, 문턱 없이** — 실서버에서 "이 Mac의 메모" 문은 지금 숨김(capabilities로 서버가 못 여는 문은 안 그림). 다시 여는 계획, 단계별로:
   - **1단계 — 개인화된 한 줄 명령**: 칩을 되살리되 누르면 안내 카드 — "터미널에 이 한 줄을 붙여넣으세요". 명령엔 방문자의 워크스페이스 ID·초대 토큰이 미리 들어가 있고, 실행하면 그 Mac에서 osascript로 메모를 뽑아 기존 capture 배치 API로 업로드 → 브라우저로 돌아오면 리빌. 기존 부품(appleNotes 추출, capture/batch, 초대 헤더 인증) 조립이라 하루 안쪽. 지금 테스터층(개발자·얼리어답터)에겐 충분한 문턱.
   - **2단계 — 메뉴바 helper 앱**: 터미널조차 안 여는 유저용. 내려받아 로그인(초대 링크 클릭)하면 메모·스크린샷 폴더를 조용히 동기화. PWA 공유 타깃과 함께 "연동은 앱이, 대화는 웹이" 구도.
   - 원칙: 어떤 단계에서도 문이 501을 만들지 않는다 — 서버가 못 여는 문은 그리지 않고, 그리는 문은 끝까지 열린다.

## AI 트랙 (ML 엔지니어용 — 상세는 멀티모달 매핑 문서)

원칙: 상황이 모델을 고른다 · 한국어 1급 시민 · 로컬-퍼스트 · 임베딩 공간은 하나.

- **Phase 1 텍스트 품질 (지금)**: **시맨틱 검색** — 지도·커맨드바 검색이 서버의 하이브리드 검색(FTS+벡터 융합, Ask가 쓰는 것)을 타게 노출. 현재 검색 UI는 키워드 매칭뿐이라 "빵 굽기"로 "사워도우"를 못 찾는다 → 임베딩 ko 벤치마크(voyage-3.5 vs text-embedding-3-small vs bge-m3) → reranker 도입(검색·관련성 공용) → 모델 티어 분리(추출=Haiku급, 명명·Ask=Sonnet급)
- **Phase 2 이미지**: Apple Vision OCR 로컬 게이트 → VLM scene_description → IG 썸네일 이해
- **Phase 3 음성·영상**: 온디바이스 STT(whisper.cpp) → 릴스(Gemini 평가)
- **Phase 4 온디바이스 티어 + MCP**: 로컬 모델 스택(Qwen3-4B/bge-m3), AI 대화 로그 수집
- **검수 신호 확장**: v2 discard 용어의 키워드 렌즈 가중 ↓ / edit 표현 가중 ↑ → v3 워크스페이스 취향 프로파일(카테고리 입도·명명)
- 각 Phase 산출물: ko 포함 벤치마크 + 기능당 원가($/1,000 기억) + 게이트웨이 미터링 단위

### 저비용·로컬 대안 표 (검토 대상 — 벤치마크로 판정)

| Task | Current | Alternatives | Mado 메모 | Ref |
|---|---|---|---|---|
| Read screenshots | gpt-4.1 | PaddleOCR | Apple Vision(맥 온디바이스)과 비교 필요; PaddleOCR은 서버측 후보 | [1] |
| Extract memories | gpt-4.1 | Small LLM + rules | 검수 신호 few-shot과 결합 시 소형 모델 품질 상승 여지 | [2] |
| Embed text | voyage / text-embedding-3-small | text-embedding-3-small or local MiniLM (all-MiniLM-L6-v2) | ⚠ MiniLM-L6은 영어 중심 — ko는 bge-m3가 로컬 후보 | [3] |
| Name categories | gpt-4.1 / claude-opus-5 | KeyBERT / RAKE / YAKE | 이미 TF-IDF 폴백(nameTokens) 구현됨 — 같은 계열, ko 조사 처리 포함 | [4] |
| Answer questions | gpt-4.1 / claude-opus-5 | Small LLM or extractive answer | 인용 계약(검증된 [n]만) 유지가 전제 — extractive는 거부율 상승 예상 | [5] |

**Ref**
[1] Li et al. (2022), PP-OCRv3, arXiv. · [2] Busta & Oyler (2025), small language models for structured extraction, Quantitative Plant Biology. · [3] Pavlyshenko & Stasiuk (2025), transformer sentence embeddings, Electronics and Information Technologies. · [4] Nadim, Akopian & Matamoros (2023), unsupervised keyword extraction tools, IEEE Access. · [5] Qian et al. (2025), VeriCite for citation-aware RAG, ACM SIGIR Asia Pacific.

## 장기 베팅

- **Meta TYI(정보 전송) 목적지 등록** — 성사 시 인스타 "진짜 연동". 파트너 심사 필요, 요건 조사부터
- **노션 정리본 내보내기** — 필요 시 POST /v1/pages (부모 페이지 연결 전제)
- **MCP 서버** — 에이전트들의 기억 계층

## 원칙 리마인드 (결정된 것, 흔들지 않기)

- 답변 페르소나는 **기억 자신** (회상체) · 질문과 저장은 **다른 표면** · placeholder는 …로 끝내지 않음
- Mado는 남의 원본을 절대 지우거나 고치지 않는다 — 표시와 링크까지, 삭제는 유저의 손
- 검수는 제안이지 숙제가 아니다 — 지도는 검수 없이도 완성돼 있다
