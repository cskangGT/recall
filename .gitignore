node_modules
dist

# Claude Code's scratch space, including git worktrees. A worktree is a repo
# inside the repo; committing one produces a directory nobody can check out.
.claude/
.DS_Store
test-results
playwright-report
*.local

# API keys. *.local already covers .env.local; these are belt and braces.
.env
.env.*
!.env.example
